[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "et-has"
version = "0.1.0"
description = "File snapshot and history tracking library"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]